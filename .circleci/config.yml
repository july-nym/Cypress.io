version: 2.1

orbs:
  docker: circleci/docker@1.5.0

jobs:

  build: 
    docker: 
      - image: loadimpact/k6:latest
    environment:
      TERM: xterm
    working_directory: ~/app

    steps:
      - checkout
      - attach_workspace:
          at: ~/
      - run: docker run -i loadimpact/k6 run - < /Users/reposado/k6/script.js




workflow:
  test:
    jobs:
      - build